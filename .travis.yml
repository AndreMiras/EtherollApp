sudo: required

language: generic

services:
  - docker

before_install:
  - docker build --tag=etheroll .

script:
  - docker run -e ETHERSCAN_API_KEY etheroll /bin/sh -c '. venv/bin/activate && make test'
