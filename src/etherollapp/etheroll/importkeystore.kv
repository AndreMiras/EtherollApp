<ImportKeystore>:
    orientation: 'vertical'
    MDLabel:
        halign: 'center'
        text: 'Copy it in to the keystore directory.\n{}'.format(root.keystore_path)
        theme_text_color: 'Primary'
    PushUp:
